import { Box } from "@chakra-ui/react";
import { useData } from "../../../../utils/fetchData";

const ViewEvents: React.FC = () => {
    // const { data, errror } = useData<EventData>("event");

    return <Box></Box>;
};

export default ViewEvents;
